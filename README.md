# CLAUDE Ledger (Electron + React 骨架)

最小可运行的 Electron + React 项目骨架，使用 Vite 作为前端构建工具。

快速开始：

```bash
# 安装依赖
npm install

# 启动开发环境（会启动 Vite 和 Electron）
npm run dev

# 构建静态文件
npm run build

# 以构建结果启动
npm start
```

注意：第一次运行请确保网络可用以安装依赖。